apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Docker 网络中使用服务名访问
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: '15s'
      httpMethod: POST
